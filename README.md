dotn run the code(you will run into a lot of errors)!!! now the project in progress
when it's completed, I will share a bash command to run the scanner.cpp with respect to the libraries (this code uses openCV and librencurses)
